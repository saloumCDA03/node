/**
Le module HTTP de Node.js est un module de base qui permet de créer un serveur HTTP. Le protocole HTTP régit les échanges entre un client HTTP (par exemple, un navigateur web) et un serveur HTTP.

Une requête HTTP est un texte qui contient des en-têtes (informations sur le client, etc.) et des données (souvent présentées sous forme de chaîne de caractères). Une réponse HTTP est également un texte qui contient des en-têtes (informations sur le serveur, etc.) et des données (généralement du code HTML).

Pour produire une requête HTTP, on peut saisir une URL dans la barre d'adresse du navigateur, cliquer sur un lien ou soumettre un formulaire. Le navigateur web construit alors la requête HTTP et l'envoie au serveur.

Pour produire une réponse HTTP en réponse à une requête HTTP, on peut créer un serveur HTTP en utilisant le module HTTP de Node.js. Le serveur doit être programmé pour construire la réponse HTTP appropriée en fonction de la requête reçue. La documentation officielle du module HTTP de Node.js se trouve ici : https://nodejs.org/dist/latest-v5.x/docs/api/http.html
**/
